import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        navy: "#0B1F3B",
        emerald: "#10B981",
      },
    },
  },
  plugins: [],
};

export default config;
